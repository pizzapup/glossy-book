import "./Link.css";

/** Links should always be underlined (blue recommended). Mark external links with text ("external") or an icon with screen-reader-readable text. <br/> [When to use `target='blank'`](https://css-tricks.com/use-target_blank/) by Chris Coyier
 * - Links - take you to a new page
 * - Buttons - trigger some kind of interactive action related to the content on the current page
 *
 */
export const Link = (props) => {
  return <>Link</>;
};
